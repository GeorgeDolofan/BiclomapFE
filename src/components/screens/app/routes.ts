const ROUTES = {
  HOME: 'home',
  SIGNUP: 'sign-up',
  APP: 'app',
  MAP: 'map',
  TRACKS: 'TRACKS',
};

export const ROUTE_APP = `/${ROUTES.APP}`;

export const ROUTE_HOME = `/${ROUTES.APP}/${ROUTES.HOME}`;

export const ROUTE_SIGNUP = `/${ROUTES.APP}/${ROUTES.SIGNUP}`;

export const ROUTE_MAP = `/${ROUTES.APP}/${ROUTES.MAP}`;

export const ROUTE_TRACKS = `/${ROUTES.APP}/${ROUTES.TRACKS}`;

export default ROUTES;
